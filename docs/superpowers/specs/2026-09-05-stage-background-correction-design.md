# Cover stage definition correction

User explicitly confirmed: the cover itself fills the entire stage background; lyrics overlay it. The previous cover-plane plus separate reading region was the wrong product definition and is superseded. Continue the reviewed experience branch so all other accepted improvements remain available.

Use one aspect-fill background cover at every window shape, with position/zoom adjusting its crop and gentle optional softening. Preserve image proportions, accept natural edge cropping when cover and window differ, and do not allocate a separate cover rectangle. A modest dark veil supports lyric contrast; lyrics occupy a centered reading viewport over the cover, with the existing bottom HUD. Keep the normal full-document scroll/preview behavior. Ambient/classic, capsule, desktop and playback are outside this correction.

Validation: geometry must cover every canvas point for square/portrait/landscape at normal/minimum/portrait windows; reading remains inside window and independent of cover aspect. Native bright/wide/compact renders must show cover behind the lyrics, not only to one side. Debug/Release, focused existing contracts, source review, then a distinct preview package. User design approval is their explicit “对” to the overlay/full-stage proposal; no further approval gate is needed.
