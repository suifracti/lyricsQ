# Progress ledger

2026-09-05: started goal; no prior goal turn to classify. Fresh origin/main fb95a9e verified. Created codex/experience-restoration. Reference research delegated read-only. Initial design/plan records full scope. User authorized autonomous iteration and reopened prior presentation freezes.

2026-09-05 integration: reference inventory recovered; reliability implementation and independent review pass five focused/native test suites. Capsule production v4 and floating interaction/readability first iteration compiled. Native visual host now uses generated HTTP artwork and isolated settings/database. First stage portrait render reveals excessive empty space and undersized artwork, returned for visual iteration. Shared host building for floating/capsule runtime and style matrix. No final product acceptance claimed. Obsidian active Handoff updated with explicit expanded user scope and current uncommitted source identity.

User addendum: direct boring.notch reuse requested if licensed. Upstream GPL-3.0 verified; project currently all-rights-reserved. Asked whether user accepts GPL distribution for direct GPL implementation; pending answer, investigating explicitly MIT-licensed original DynamicNotchKit foundation listed by boring.notch. No project-license change or GPL import yet. Continues alongside original reliability/presentation goal.

User second addendum supersedes prior direct-reuse investigation: if attribution required, research and implement independently. MIT import is being removed completely, including copied source, notices and project/test entries; no overall license change. Earlier MIT host/release build is superseded and will not be delivered. Native floating real unlock/relock now PASS with CUA + controller readback. Stage r4 native readability PASS; final preview gate reviewed.

## Final integrated preview

Desktop second iteration native five-case matrix passed; actual style single/theme changes and lock/unlock passed. Capsule independent expanded/collapsed renders inspected; More popover opened and held, action dismissed correctly. Final capsule/desktop review has no actionable findings. All 14 targeted suites and final production Debug/fresh Release builds passed. Source hashes match the final isolated host manifest. Final source/evidence is prepared for an independent preview package; no main merge or release update. Hardware notch/multiscreen/long real playback remain manual limits.

2026-09-06 follow-up started: user requested two UI corrections on `codex/lyrics-search-ambient-fix`: restrict ambient/classic playback-detail hover to the actual cover/player region (including its gap, portrait header-aware, with interaction/panel pinning) and move stage lyric scroll anchor down by a bounded ~5%/48pt. Existing uncommitted `Tests/v3_responsive_geometry_contract.swift` was inspected and preserved; its new assertions currently fail because the production helpers are not yet present. Formal root remains untouched.

2026-09-06 v2 preview continuation: hidden playback details now apply a bounded visual-only cover scale; the measured hover anchor and lyrics layout frames remain static. Stage hover uses the full canvas with a bounded midpoint hysteresis band. Static ambient/classic/portrait and stage hidden/visible comparisons were regenerated from the current source; the active stage line remains below the prior anchor without HUD overlap. CUA verified lyric-text hide, but its drag-only input cannot reliably prove move-only return to the cover/player region or stage lower-half reveal after release; those remain explicitly unverified. The current Release preview v2 is open with v1 and the older preview retained. No commit, push, merge, formal-app replacement, or real Spotify transport action occurred.

2026-09-06 follow-up implementation: added measured playback-region reporting for ambient/classic adaptive and portrait layouts; kept stage/compact-focus canvas fallback and toolbar state independent; pinned playback details during slider interaction and tool popovers; stage `scrollTo` now uses `min(0.47 + 5%, 0.47 + 48pt / viewportHeight)`. Geometry contract moved RED→PASS. The scroll-stability contract first failed on stale exact-string checks for `.leading` and `state.liveLyrics`; those checks were updated to assert the eager stack, selected document snapshot, shared `currentIndex` boundary, stable row key, and no playback-tick scrolling, then passed. Native isolated events verified lyric-side hide and cover/player-side reveal for ambient/classic landscape, plus portrait lyric-side hide with the final top-header region; hover-off renders kept playback details always visible. The isolated CUA path could not hold an AppKit progress drag long enough for a reliable mid-drag observation, so that one interaction remains unverified. Stage before/after, short-window, and native-fullscreen renders were inspected; Debug and Release builds passed. No real Spotify transport was touched.

2026-09-06 A–H continuation: retained the dirty worktree and connected the requested restoration pieces. Translation paste now offers a preview and fail-closed matching for timestamped fixed offsets and untimed exact-line imports; confirmed manual edits feed a local style profile summary/examples. Codex/ChatGPT translation uses the installed app-server protocol with a stable line-ID response, read-only timestamps/context, and no application access to raw OAuth credentials; a live one-line structured translation passed after adapting to the current server's object-root schema requirement. Presentation-only lyric offset is shared by the main, stage, fullscreen, floating and related clock paths without changing authoritative Spotify time. Desktop settings expose the same offset and immediate second-line selection. Ambient/classic cover enlargement remains visual-only and bounded; stage keeps the lower lyric anchor and upper/lower playback split. Focused contracts, `git diff --check`, and the final Debug build passed. The independent Debug preview and build-info record are at `/Users/apple/Downloads/SpotifyLyrics-AH-Preview-20260906-Debug.app` and `/Users/apple/Downloads/SpotifyLyrics-AH-Preview-20260906-Debug.build-info.txt`; the older preview remains on disk. No commit, push, merge, formal-app replacement, or real Spotify transport action occurred.

Preview handoff status: the old running preview was inspected and showed only the main Lyric Island window, with no lyrics editor or unsaved edit surface visible; it was not forcibly closed before this handoff. Actual move-only AppKit pointer paths for cover→card, portrait lyrics→header, stage lower-half reveal, reverse animation, and leaving the slider region during an active drag remain explicitly unverified; the preview is provided for user confirmation rather than marked fully accepted.

2026-09-06 final preview refresh: user-visible `appleMusicImmersiveV3` naming is now “专辑沉浸 V3” and its Catalog version is `3`; stable ID and legacy `immersiveSplit` are unchanged. Naming contract and `git diff --check` passed. A new Debug preview was built from the dirty activity worktree and launched by its full path: `/Users/apple/Downloads/SpotifyLyrics-AH-Preview-20260906-V3-Debug.app`, SHA-256 `4b4322ff4b5fd89ff8ff23da6812d8d417db1c0fd1817f6c4d069fd2ef22c6ed`; build record: `/Users/apple/Downloads/SpotifyLyrics-AH-Preview-20260906-V3-Debug.build-info.txt`. No commit, push, merge, or full regression was performed; the existing pointer-path and subjective stage-placement limits remain unverified.
